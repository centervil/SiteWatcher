{
  "name": "sitewatcher-frontend",
  "version": "1.0.0",
  "scripts": {
    "start": "http-server -p 3000",
    "build": "mkdir -p build && cp index.html styles.css app.js build/ && cp -r assets build/"
  },
  
  "dependencies": {
    "http-server": "^0.12.3"
  }
} 