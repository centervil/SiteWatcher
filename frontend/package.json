{
  "name": "sitewatcher-frontend",
  "version": "1.0.0",
  "scripts": {
    "start": "http-server ./build -p 3000",
    "build": "rm -rf build && mkdir build && cp index.html styles.css build/"
  },
  "dependencies": {
    "http-server": "^0.12.3"
  }
}
