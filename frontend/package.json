{
  "name": "sitewatcher-frontend",
  "version": "1.0.0",
"scripts": {
  "start": "http-server -p 3000",
  "build": "rm -rf build && mkdir build && cp index.html styles.css app.js build/ || { echo 'Build failed'; exit 1; }"
},
  "dependencies": {
    "http-server": "^0.12.3"
  }
}
